# ARCHITECTURE.md — System/Data Reasoning Reference (JASIRI NET OPS)

**Status: placeholder.** Not written yet. To be drafted by Claude from the
existing schema/infra/integration decisions once Phase 0 gives a clear
view of the codebase — capturing the WHY behind decisions that already
exist, not proposing new ones. Reviewed and corrected by the owner
afterward.

Every section below currently says "(not yet determined)" as a literal
placeholder. Phase 0 is done, on this front, when every section has real
content instead of that placeholder text, AND the owner has reviewed each
section and either approved it or corrected it.

## Schema choices
(what the data model looks like, and why it's shaped that way)
(not yet determined)

## Infra / hosting choices
(where this runs, why that choice was made)
(not yet determined)

## Integration approach
(how the isolated domains talk to each other — the shared permission
function, any other cross-domain calls)
(not yet determined)

This file rarely changes once written. CLAUDE.md references it rather
than repeating it.