# DESIGN.md — UI/UX Reference (JASIRI NET OPS)

**Status: placeholder.** Not written yet. To be drafted by Claude from the
existing frontend's actual CSS/component patterns once Phase 0 gives a
clear view of the codebase — then reviewed and corrected by the owner, not
authored from scratch by either party guessing at conventions that don't
exist yet.

Every section below currently says "(not yet determined)" as a literal
placeholder. Phase 0 is done, on this front, when every section has real
content instead of that placeholder text, AND the owner has reviewed each
section and either approved it or corrected it.

## Accent / color theme
(not yet determined)

## Typography
(not yet determined)

## Component style conventions
(e.g. button shapes, card layouts, form field style)
(not yet determined)

## Spacing conventions
(not yet determined)

## Mobile behavior
(deferred until Phase 1 — leave as "not yet determined" until then, that's
expected, not a gap)